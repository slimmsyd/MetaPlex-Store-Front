#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@bundlr-network/client/build/src/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../@bundlr-network/client/build/src/cli.js" "$@"
  ret=$?
fi
exit $ret
