#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sort-deep-object-arrays/bin/sort-deep-object-arrays.js" "$@"
  ret=$?
else 
  node  "$basedir/../sort-deep-object-arrays/bin/sort-deep-object-arrays.js" "$@"
  ret=$?
fi
exit $ret
